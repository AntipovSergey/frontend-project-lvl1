name: GitHub Actions Lint
on: push
jobs:
  Lint project:
    runs-on: ubuntu-latest
    steps:
       - name: npm install
        # Install project
        run: make install
      - name: Run linter
        # Run Linter
        run: make lint
